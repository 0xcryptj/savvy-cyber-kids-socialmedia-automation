"use client";
import { FormEvent, useState } from "react";
export default function ForgotPasswordPage(){const [email,setEmail]=useState("info@savvycyberkids.org");const [sent,setSent]=useState(false);async function submit(e:FormEvent){e.preventDefault();await fetch("/api/auth/reset/request",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});setSent(true)}return <main><section className="card settings-panel" style={{maxWidth:480,margin:"15vh auto"}}><p className="eyebrow">SAVVY CYBER KIDS</p><h2>Reset your password</h2>{sent?<p>If the address is eligible, a reset link has been emailed to it.</p>:<form onSubmit={submit} className="settings-grid"><label className="settings-wide">Account email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required /></label><button>Send reset email</button></form>}</section></main>}
