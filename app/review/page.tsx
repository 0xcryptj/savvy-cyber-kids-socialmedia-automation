"use client";
import { useState } from "react";
import { pendingPost } from "@/src/lib/mock-data";
export default function ReviewPage(){const [status,setStatus]=useState(pendingPost.status); const [caption,setCaption]=useState(pendingPost.caption); return <><div className="page-intro"><div><p className="eyebrow">CONTENT PIPELINE / REVIEW</p><h2>Pending review</h2><p>One thoughtful pass before this post moves to the approved queue.</p></div><span className="count">1 item</span></div><div className="review-layout"><article className="card"><div className="post-main"><div className="graphic"><img src={pendingPost.graphicPath} alt="Generated social graphic preview" /></div><div className="post-copy"><span className="status">{status.replace("_", " ")}</span><h2>{pendingPost.topic_heading}</h2><p className="meta">{pendingPost.publishedAt} · <a href={pendingPost.sourceUrl}>Open source article ↗</a></p><h3>{pendingPost.article_title}</h3><textarea className="edit-area" value={caption} onChange={e=>setCaption(e.target.value)} aria-label="Social caption" /><p className="hashtags">{pendingPost.hashtags.join("  ")}</p></div></div><div className="actions"><button onClick={()=>setStatus("APPROVED")}>Approve</button><button className="secondary" onClick={()=>setStatus("REVISION")}>Save edit</button><button className="outline" onClick={()=>setStatus("REJECTED")}>Reject</button><button className="outline" onClick={()=>setCaption(pendingPost.caption)}>Regenerate caption</button><button className="outline">Regenerate graphic</button></div></article><div className="side-stack"><div className="card side-card"><p className="eyebrow">MANUAL FALLBACK</p><h3>Everything you need to post</h3><p>Download the graphic, copy the caption, or take the article URL to your usual scheduler.</p><div className="quick-links"><a href={pendingPost.graphicPath} download>Download graphic <span>↓</span></a><a href="#caption">Copy caption <span>↗</span></a><a href="#hashtags">Copy hashtags <span>↗</span></a><a href={pendingPost.sourceUrl}>Open article <span>↗</span></a></div></div><div className="card side-card"><p className="eyebrow">DESIGN HANDOFF</p><h3>Canva template pending</h3><p>The branded Canva template URL will be connected here when access is confirmed.</p><a className="button secondary" href="/settings">View connector status</a></div></div></div></>}
