export default function Settings(){return <><div className="page-intro"><div><p className="eyebrow">WORKSPACE / CONFIGURATION</p><h2>Connector status</h2><p>Secrets are environment-only and never stored in the database.</p></div></div><div className="queue-list">{[["RSS ingestion","Ready","HTTP feed discovery"],["OpenAI","Pending","OPENAI_API_KEY not configured"],["Canva","Pending","Template URL/access pending"],["Make.com","Pending","MAKE_WEBHOOK_URL not configured"],["SocialBee","Planned","Handled through Make.com"],["Publishing","Safe","AUTO_PUBLISH=false · approval required"]].map(([name,status,note])=><div className="card queue-row" key={name}><div><h3>{name}</h3><p>{note}</p></div><span className={status==="Ready"||status==="Safe"?"status":"count"}>{status}</span></div>)}</div></>}
