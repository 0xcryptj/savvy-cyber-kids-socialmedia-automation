import { approvedPosts } from "@/src/lib/mock-data";
export default function QueuePage(){return <><div className="page-intro"><div><p className="eyebrow">CONTENT PIPELINE / HANDOFF</p><h2>Approved queue</h2><p>Approved locally, ready for a deliberate handoff to Make.com.</p></div><span className="count">{approvedPosts.length} item</span></div><div className="queue-list">{approvedPosts.map(post=><div className="card queue-row" key={post.id}><div><span className="status">{post.status}</span><h3>{post.topic_heading}</h3><p>{post.article_title}</p></div><div><button>Queue for Make</button><button className="outline">View package</button></div></div>)}</div><div className="card side-card section"><p className="eyebrow">SAFE HANDOFF</p><h3>Make.com is not the source of truth.</h3><p>This queue remains local. A configured webhook will receive an approved post package only after an explicit handoff.</p></div></>}
