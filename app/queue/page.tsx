import { listPosts } from "@/src/workspace/store";
import { PostizBulkScheduler } from "./PostizBulkScheduler";
export default async function QueuePage(){const posts=await listPosts("APPROVED");return <><div className="page-intro"><div><p className="eyebrow">CONTENT PIPELINE / SCHEDULING</p><h2>Ready to post</h2><p>Preview approved graphics and captions, select the posts you want, then schedule them together in Postiz.</p></div><span className="count">{posts.length} item{posts.length === 1 ? "" : "s"}</span></div>{posts.length ? <PostizBulkScheduler posts={posts} /> : <div className="card empty">No approved posts yet. Review a generated post to move it here.</div>}<div className="card side-card section"><p className="eyebrow">POSTIZ SCHEDULING</p><h3>One workspace for the final handoff.</h3><p>Every selected post keeps its own graphic, caption, hashtags, and Postiz schedule. Nothing is sent until you confirm the selected posts and channels.</p></div></>}
