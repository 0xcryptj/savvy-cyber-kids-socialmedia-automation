# Social post generation

Given only the article title and body, return JSON matching `schemas/social-post.schema.json`. Preserve the article title exactly. Generate one concise graphic-friendly topic heading, an educational and positive caption, and exactly two topical hashtags. Never generate `#savvycyberkids` or `#cyberhero`; application logic appends those fixed hashtags.
